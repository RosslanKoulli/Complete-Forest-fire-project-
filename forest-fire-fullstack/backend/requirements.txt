# Forest Fire Prediction API -- backend requirements
# Pin major versions; sub-pin loosely for security updates.

fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic>=2.5.0,<3.0.0
python-multipart>=0.0.6

# ML stack -- these versions need to match what the trained models
# were saved with. If the existing project uses different versions,
# match those here.
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<3.0.0
imbalanced-learn>=0.11.0
shap>=0.44.0
joblib>=1.3.0

# Numerical
numpy>=1.24.0,<3.0.0
pandas>=2.0.0,<3.0.0
scipy>=1.11.0
requests>=2.28
